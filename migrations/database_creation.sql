CREATE TABLE IF NOT EXISTS `user`
(
    id       int,
    lastname varchar(80),
    name     varchar(80),
    birtdate date,
    email    varchar(80)
);


CREATE TABLE IF NOT EXISTS `company`
(
    id       int,
    lastname varchar(80),
    name     varchar(80),
    birtdate date,
    email    varchar(80)
);